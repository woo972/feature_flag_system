server:
    port: 8082

spring:
    config:
        activate:
            on-profile: local
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true

app:
    database:
        host: ${DATABASE_HOST:localhost}
        username: ${DATABASE_USERNAME:postgres}
        password: ${DATABASE_PASSWORD:postgres}
